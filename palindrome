def is_palindrome(s):
s = s.lower().replace(" "," ")
return s == s[::-1]
user_input = input("Enter a string:")
if is_palindrome(user_inpt):
print(f"{user_input}"is a palindrome.")
else:
print(f"{user_input}"is not a palindrome.")
